version: 2

models:
  - name: customers
    description: Each record represents a customer.
    columns:
      - name: customer_id
        description: "{{ doc('customer_id') }}"
        tests:
          - unique
          - not_null
      - name: first_name
        description: A customer's first name.
      - name: last_name
        description: A customer's last name.
      - name: count_orders
        description: The number of orders a customer has had all-time.
      - name: first_order_at
        description: The timestamp of a customer's first order.
      - name: most_recent_order_at
        description: The timestamp of a customer's most recent order.

  - name: orders
    description: Each record represents an order.
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
 
  - name: deliveries
    description: Each record represents a customer's all-time delivery summary, including counts of successful, failed, and other-status deliveries.
    columns:
      - name: customer_id
        description: "{{ doc('customer_id') }}"
        tests:
          - unique
          - not_null
      - name: total_deliveries
        description: The total number of deliveries associated with a customer across all orders.
        tests:
          - dbt_utils.expression_is_true:
              expression: "= successful_deliveries + failed_deliveries + other_status_deliveries"
      - name: successful_deliveries
        description: The number of deliveries with a status of 'delivered'.
      - name: failed_deliveries
        description: The number of deliveries with a status of 'cancelled'.
      - name: other_status_deliveries
        description: The number of deliveries with a status other than 'delivered' or 'cancelled'.
      - name: last_delivery_date
        description: The date of the customer's most recent successful delivery. Null when a customer has no successful deliveries.
      - name: fulfillment_ratio
        description: The ratio of successful deliveries to total deliveries for a customer. Null when a customer has no deliveries.
        tests:
          - dbt_utils.expression_is_true:
              expression: "between 0 and 1"