version: 2

models:
  - name: customers
    description: Each record represents a customer.
    columns:
      - name: customer_id
        description: "{{ doc('customer_id') }}"
        tests:
          - unique
          - not_null
      - name: first_name
        description: A customer's first name.
      - name: last_name
        description: A customer's last name.
      - name: count_orders
        description: The number of orders a customer has had all-time.
      - name: first_order_at
        description: The timestamp of a customer's first order.
      - name: most_recent_order_at
        description: The timestamp of a customer's most recent order.
        
  - name: orders
    description: Each record represents an order.
    columns:
      - name: order_id
        tests:
          - unique
          - not_null

  - name: deliveries
    description: Each record represents the delivery statistics for a customer, including total, successful, failed, and other-status deliveries.
    columns:
      - name: customer_id
        #description: "{{ doc('customer_id') }}"
        tests:
          - unique
          - not_null

      - name: total_deliveries
        #description: "{{ doc('total_deliveries') }}"
        tests:
          - not_null

      - name: successful_deliveries
        #description: "{{ doc('successful_deliveries') }}"
        tests:
          - not_null

      - name: failed_deliveries
        #description: "{{ doc('failed_deliveries') }}"
        tests:
          - not_null

      - name: other_status_deliveries
        #description: "{{ doc('other_status_deliveries') }}"
        tests:
          - not_null

      - name: last_delivery_date
        #description: "{{ doc('last_delivery_date') }}"

      - name: fulfillment_ratio
        description: "The ratio of successful deliveries to total deliveries for the customer."